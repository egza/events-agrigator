# events-agrigator
group project

  init commands

  install node and tools once:

npm install -g bower grunt


  install project dependencies:

npm install
bower install


  run project:
  
npm start
